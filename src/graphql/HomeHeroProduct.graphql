# The product the homepage hero photo shows, with the listing fields the exact-locale check
# needs, so its floating card is only ever this market's name, URL, price and photo.
query HomeHeroProduct($id: ID!, $channel: String!, $lang: LanguageCodeEnum!) {
	product(id: $id, channel: $channel) {
		...ProductListItem
	}
}
